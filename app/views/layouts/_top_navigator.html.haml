%nav.navbar.navbar-toggleable-md.bg-faded.sticky-top
  %a.navbar-brand{:href => "/"}
    В помощь оператору ;)
  .collapse.navbar-collapse
    .navbar-nav.navbar-toggler-right
      - if user_signed_in?
        %a.nav-item.nav-link.active{:href => user_path(current_user)}
          = current_user.email
        %a.nav-item.nav-link.active{:href => destroy_user_session_path}
          Выйти
      - else
        %a.nav-item.nav-link.active{:href => new_user_session_path}
          Вход
        %a.nav-item.nav-link.active{:href => new_user_registration_path}
          Регистрация